apiVersion: v1
kind: Pod
metadata:
  name: env-pod
  labels:
    env: dev
    type: test
spec:
  containers:
    - name: env-pod
      image: nginx
      ports:
        - containerPort: 80
      env:
        - name: env-pod
          valueFrom:
            configMapKeyRef:
              name: env-config-map
              key: production
        - name: env-pod-2
          valueFrom:
            configMapKeyRef:
              name: env-config2-map
              key: project
